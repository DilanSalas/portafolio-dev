---
import StudiesItem from './StudiesItem.astro';

const studies = [
  { key: 'study_1' },
  { key: 'study_2' },
];
---

<ol class="flex flex-col items-center sm:items-start space-y-6">
  {studies.map((study) => (
    <StudiesItem i18nKey={study.key} />
  ))}
</ol>
